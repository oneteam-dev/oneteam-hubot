<%= @env %> deployment
<%= @hr @env.length + 11, '=' %>

Created by @<%= @envelope.user.user_name %> in [<%= @envelope.room.title %>].

Continue by clicking _Merge pull request_ button, then deployment build will start in [CircleCI].

After deployment
----------------

Please check after operation check.

<%= @checkList.join "\n" %>

[CircleCI]: <%= @ciURL %>
[<%= @envelope.room.title %>]: https://oneteam.one-team.io/topics/<%= @envelope.room.number %>
